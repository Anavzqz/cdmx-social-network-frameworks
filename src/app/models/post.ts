export interface PostInterface {
    id?: string;// el signo de interrogación es para decir que no es un campo requerido!
    description?: string;
    date?: any;
    userId?: string;
    userName?: string;

}